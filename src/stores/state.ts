// OAuth state store removed — provide minimal stub to avoid runtime import errors
export const storeState = async (_key: string) => {};
export const validateAndDeleteState = async (_key: string) => false;

