// OAuth support removed. This file intentionally left as a no-op to avoid
// breaking imports from other modules that might still reference it.
export default function registerOAuth(_server: any) {
  // no-op
}
